# Skills.jsx

`src/components/Skills.jsx`

> ⚠ **Not imported anywhere** — unreachable from `src/main.jsx`. Treat as dead code; edits here won't affect the live site.

_No description available. Renders a section with `id="skills"`._

## Imports
- `SectionHeader` from `./SectionHeader`

## Exports
### `Skills()`
No props.

## Local data constants
- `SKILL_GROUPS`

